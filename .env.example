# OSA QC Automation — Environment Variables
# Copy this file to .env and fill in your values

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=your_database_name

# Optional: number of concurrent browser contexts (default 1 = sequential)
MAX_CONCURRENCY=1
